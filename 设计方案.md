# 英语词汇学习助手设计方案

更新日期：2026-09-14

状态：方案已确认，第一版已实现。实现时根据本机音色兼容性增加 Windows 系统语音回退；真实 DeepSeek 内容质量待配置密钥后验证。

## 1. 已确认的范围

- 使用方式：本机网页，个人使用。
- 输入：当前示例这种可复制文字、带题号和 A-D 四个选项的英语词汇填空 PDF。
- 内容：沿用现有两份 Markdown 的结构，提高准确性，每条搭配可独立点读。
- 大模型服务：DeepSeek。
- 朗读：浏览器或系统提供的英语语音。
- 划词：应用页面内任意英文选区均可查词、查搭配或翻译整句；离线词典优先，DeepSeek 补充语境释义。
- 选项解释：A-D 每个选项都必须有简要解释，包括全部错误项。
- 第一版集中完成导入、生成、阅读、点读、纠错和导出流程。

## 2. 用户流程

1. 首次使用时在本机配置 DeepSeek API Key，选择模型和可用英语音色。
2. 拖入 PDF，也可通过文件选择器导入。
3. 自动解析，显示题数、来源页码和异常题目。用户可查看 PDF 并修正解析结果。
4. 点击“生成笔记”。结构完整的题目进入生成队列，异常题目修正后可继续生成。
5. 通过校验的题目逐题显示；进度展示已完成数、失败数和待处理数。
6. 用户阅读、点击朗读、修改解释，或对指定题目重新生成。
7. 自动保存内容和阅读位置，需要时导出 Markdown。

同一 PDF 重复导入时提示打开已有文档或创建副本。已保存内容在关闭网页或重新启动应用后仍可访问。

## 3. 脚本和模型的职责

| 环节 | 脚本负责 | 模型负责 |
|---|---|---|
| 原文解析 | 标题、题号、题干、填空区间、A-D 选项、来源页码 | 无 |
| 内容补全 | 构造输入及固定输出字段 | 推荐答案、四个选项各自的词性、中文释义、搭配和简要解释 |
| 内容检查 | 题目 ID、字段类型、选项完整性、答案有效性、非空内容 | 遇到语义歧义时给出简短说明 |
| 展示与导出 | 根据答案替换原填空区间、加粗答案、生成界面及 Markdown | 无 |

模型输出结构化 JSON。脚本保留原始题干和选项，不接受模型返回的整句改写作为原文。

没有参考答案时，答案来源标记为“模型推断”。结构校验不能证明释义、搭配或选项判断正确，界面保留纠错入口；有争议的题目可标为“待核对”。不把模型自报的置信度当作正确率。

## 4. 解析与原文保真

- 按 PDF 页提取文字，必要时使用文字坐标重建阅读顺序，保留页码。
- 识别题号、题干、一个填空区间及四个选项，处理换行和跨页题目。
- 使用区间替换填入答案，保留原句中其他内容和选项的原有词形。
- 只自动处理可以明确识别的排版空白；疑似断词或拼写错误保留原始提取结果，并记录修正。
- 题号重复或跳号、选项缺失、多个填空、无法定位填空等情况进入待修正状态。
- 扫描件、加密后无法读取的 PDF、非目标题型给出明确状态，用户仍能查看已成功解析的内容。
- 保留不可变的原始提取记录；用户修正解析数据时生成修订记录，重新检查受影响的笔记。

当前两份 PDF 各有 30 道题、120 个选项，未发现答案页。现有 Markdown 作为内容形式的参考，不作为全部正确的标准答案。

已发现的原文保真例子：笔记 2 第 5 题移动了 eternal 的位置，第 9 题漏了 eat。新的生成流程应避免这类改写。

## 5. 笔记内容和格式

每道题包含：

1. 题号和填入答案后的完整英文原句，答案词加粗。
2. 答案字母。
3. A-D 四项，每项包含原选项词或短语、词性、中文释义、常用搭配。
4. 每个选项都有结合本题的简要解释：正确项标记并说明成立依据；其余三项逐项说明词义、搭配或语境为何不适用，必要时比较区别。不能只写“不合”或“不符合语境”。

每个选项默认生成 1-3 条自然、常用的搭配，优先贴合题目中的义项。每项解释保持 1-2 句；生成与编辑保存校验四项解释均非空且至少 8 个字符。整句翻译通过划词查询按需获取；音标和词形信息在离线词典收录时显示。

Markdown 示例：

```markdown
**1.** Starting with the **premise** that there is life on the planet Mars, the scientist went on to develop his argument.
答案：A
- A) premise：n. 前提；假设。搭配：`on the premise that...` / `start from the premise that...`。正确。解释：科学家以“火星上存在生命”为论证前提，premise 可接 that 从句说明这一假设。
- B) pretext：n. 借口；托词。搭配：`under the pretext of...` / `a pretext for war`。解释：pretext 是为行为提供的借口，通常隐含掩饰真实动机；本题说的是科学论证的假设。
- C) foundation：n. 基础；地基；基金会。搭配：`lay the foundation for...` / `a solid foundation`。解释：foundation 泛指基础，常接 of 或 for；此处 that 从句具体说明推理前提，premise 更准确。
- D) presentation：n. 展示；陈述。搭配：`give a presentation` / `make a presentation on...`。解释：presentation 指展示或陈述活动，不表示论证赖以出发的假设，不能表达这里的前提关系。
```

网页可将同一字段分行排版，让搭配更容易点击。普通 Markdown 文件保留可读性；点读能力由学习网页提供。

## 6. 页面和交互

- 左侧：文档列表、生成状态、题号导航。
- 主区域：按题号连续展示笔记，使用清晰的段落和分隔线。
- 顶部：文档名、搜索、导出、朗读设置。
- 删除资料：标题旁提供垃圾桶入口，确认弹窗展示资料名称、题数和不可撤销的影响。清理应用内 PDF 副本、预览、笔记及其历史、生成任务与调用记录，不删除用户原文件或已导出的 Markdown；生成未结束时先停止并等待。删除当前资料后切换相邻资料，全部删除后回到可导入的空资料库。
- 题目工具：查看来源、编辑、重新生成、标记待核对。
- PDF 来源通过可展开的区域查看，定位到来源页；窄屏使用独立视图。
- 搜索支持题干、选项词、搭配和中文解释；保存每份文档的阅读位置。
- 页面内划选任何英文单词、短语或整句，出现“查词 / 翻译”浮动菜单，包含所选内容的朗读和复制。支持普通正文、选项、搭配及跨越加粗目标词的选区。
- 词典弹窗显示释义、已有音标、原形和来源，提供“语境释义”。切换选区取消旧请求；关闭或滚动正文收起弹窗，窄屏限制在可视范围内。
- 生成失败、解析异常和待核对使用不同状态，避免混淆。
- 窄屏收起侧栏，保留题号导航和学习正文。第一版服务默认仅监听本机。

## 7. 朗读规则

| 操作 | 行为 |
|---|---|
| 点击原句普通正文或句旁播放图标 | 朗读填好答案的完整英文句子 |
| 点击原句中的加粗目标词 | 只朗读该词或短语 |
| 点击任意一个选项词 | 朗读该选项，保留其原有词形 |
| 点击搭配 | 只朗读该条搭配 |
| 点击其他可朗读内容 | 停止上一段，播放新内容 |
| 再次点击正在朗读的内容或停止图标 | 停止朗读 |

单词、短语操作阻止事件向整句传播。选择文字进行复制时不触发朗读。播放期间高亮对应内容，结束、取消或失败后清除高亮。

默认优先选择本机可用的美式英语音色，没有时选择其他英语音色；提供音色选择和语速调节，默认语速为 1.0。控件支持键盘操作，图标有明确的无障碍名称。

搭配数据分别保存显示文本和朗读文本：

- `sb`、`sth` 在朗读文本中分别展开为 `somebody`、`something`。
- `conform to/with rules` 拆为 `conform to rules` 和 `conform with rules`，独立展示、独立点读。
- 省略号作为未完成结构的显示符号，朗读时不念标点名称。

直接点击词的范围是四个选项和句中的目标答案词。句中其他普通文字点击后朗读整句；划选任意词、词组或句子后，也可在划词菜单中单独朗读该选区。

优先使用 Web Speech API。浏览器未暴露本机英语音色时，可选择后端通过 Windows System.Speech 提供的本机音色，音频缓存在本机后播放。英语音色数量、质量、离线可用性由浏览器和系统决定；没有可用语音或播放失败时显示可操作的提示。网页本身不要求额外的 TTS Key。

## 8. DeepSeek 接入与生成可靠性

### 划词词典

- 离线查询：ECDICT 的 768,739 条中文词条、词形索引及内置常用搭配。采用 SQLite 索引，精确查词不需要密钥或联网。
- 模型补充：未收录表达和整句翻译自动使用 DeepSeek；词条已有通用释义时可主动查询“语境释义”。输入为所选文本及同一题目的句子；按 JSON 结构校验返回释义、用法和可选例句。
- 缓存键包含原文、语境、模型和提示词版本，保留大小写差异。失败结果不缓存，查询不会改写笔记。
- 未配置密钥时保留可用的离线释义，并给出模型设置入口。网络或模型错误可重试。
- “全局划词”范围为应用页面可选择的阅读文字，不跨其他软件或网站。PDF 原页以图片展示，查词使用旁边的解析正文；编辑输入框沿用原生选区操作。

### 笔记生成

- 官方服务地址：`https://api.deepseek.com`。
- 建议默认模型：`deepseek-flash`，模型名可以在本机设置中修改。
- 启用 JSON Output，提示词提供字段说明和样例，返回结果再经过后端结构校验。
- 题干和选项作为待分析数据传入，文档中的文字不作为系统指令执行。
- 以小批题目请求模型，校验通过后逐题保存。批次大小和并发数在实现时根据两份样例的实际响应调整。
- 空响应、截断、字段缺失等情况进行有限重试；持续失败时定位到具体题目，允许手动重试。
- 临时网络错误、限流或服务异常使用退避重试；密钥错误、余额不足等情况暂停队列并提示处理。
- 用户可停止继续生成；已完成结果保留，正在进行的请求可能已经产生费用。
- 重新启动后未完成任务显示为中断，可继续处理尚未完成的题目。
- 记录调用状态、模型、提示词版本和服务返回的用量，便于排查问题；不在日志中输出密钥。

生成前保存任务状态，生成结果按题提交，避免整份文件因单题失败而丢失。保留原始模型响应作为生成记录；前端只展示经过校验的内容。

截至本稿日期，已核对官方文档的模型名与 JSON Output 支持；尚未进行真实 API 调用，生成质量、延迟和用量需要接入后用样例验证。

## 9. 编辑和数据一致性

结构化数据是笔记的唯一内容来源，前端和 Markdown 导出都读取同一版本。

- 用户在题目编辑表单修改答案、释义、搭配和辨析，保存后重新计算完整句子。
- 每条笔记保留修订历史，并标记人工编辑的字段。
- 对人工修改过的题目重新生成时，生成候选版本供比较和采用；原版本持续保留。
- 修改答案时重新标记正确选项，原解析进入待核对状态，避免新答案沿用旧解释。
- 导出时只使用当前已采用版本；存在未完成或待核对内容时，导出前提示，文件中标明状态。
- `.md` 是导出文件。第一版在网页中编辑结构化内容，不自动将外部 Markdown 修改反向同步。
- 两份示例共 240 个选项已补充解释。升级时未修改笔记自动更新；人工修改或与原版本不同的笔记保留，并提供候选版本。迁移可重复启动且保留历史，原 Markdown 文件不改写。

核心数据包括：文档、原始提取题目、解析修订、笔记及修订、生成任务、学习位置、朗读设置。

## 10. 技术和本地存储

| 部分 | 方案 |
|---|---|
| 前端 | React + TypeScript + Vite |
| 后端 | Python + FastAPI |
| PDF 解析 | pdfplumber，结合文本和坐标信息 |
| 数据校验 | Pydantic 模型和业务校验 |
| 数据库 | SQLite |
| 文件 | 本地保存 PDF 原件和 Markdown 导出 |
| 页面朗读 | 浏览器 Web Speech API，Windows System.Speech 本机回退 |
| 划词词典 | 内置 ECDICT SQLite 词库、常见搭配及 DeepSeek 语境释义 |
| 运行方式 | 提供 Windows 本机启动入口，浏览器访问本地地址 |

API Key 仅由后端读取，建议使用项目本机 `.env` 配置，提供无密钥的配置示例，敏感配置不进入版本管理。前端仅显示是否已配置，不回传密钥。

PDF 原件和笔记保存在本机。生成时，提取后的题干与选项发送到 DeepSeek；模型查词时发送选中文字及所在题目的句子。浏览器或系统音色可能依赖网络，因此不会承诺全部离线运行。

## 11. 验收标准

1. 两份示例 PDF 各解析出 30 道题、120 个选项，题号、题干、选项和来源页码与原文一致。
2. 填答案只替换原填空区间，保留原句中其余文字；第 5 题移动词位、第 9 题漏词等已知问题不再出现。
3. 每道完成题目包含四个选项各自的词性、释义、搭配及简要解释；正确项说明依据，其他三项分别说明不适用原因。答案和原文选项匹配。
4. 原句、目标词、四个选项和搭配点读正确；切换不重叠，点击目标词不会触发整句。
5. 校验能发现漏题、缺选项、无效答案、空响应和字段缺失，并能修正或重试。
6. 中断后已完成题目保留，可继续生成；单题失败不丢失其他题目的结果。
7. 人工编辑保留历史，重新生成不会直接覆盖人工版本；修改答案后旧解析有待核对标记。
8. Markdown 导出与当前采用的笔记版本一致，能在普通 Markdown 阅读器中正常显示。
9. 刷新及重新启动应用后，文档、修改和阅读位置仍存在。
10. 桌面与窄屏界面无明显溢出或遮挡；无语音、无密钥、网络故障等状态有明确反馈。
11. 真实模型接入后，对两份样例逐题核对答案，并抽查各选项释义、搭配和辨析；发现的问题修正后再确认内容质量。
12. 任意英文正文、选项和搭配选区能唤起词典，整句可跨加粗词选择；离线单词无需密钥，语境缓存互不混用，窄屏弹窗不越界。

现有 Markdown 只用于格式对照及内容比较，不能单独证明模型答案正确。

## 12. 实施顺序

1. 解析两份 PDF，验证数据结构、原文保真和固定 Markdown 输出。
2. 接入 DeepSeek，完成字段校验、任务保存、异常处理和继续生成。
3. 完成文档管理、笔记阅读、点读、纠错、修订和导出界面。
4. 使用两份 PDF 完成实际生成与浏览器验收，提供启动入口及本机配置说明。

## 官方参考

- DeepSeek 接口与当前模型：https://api-docs.deepseek.com/
- DeepSeek JSON Output：https://api-docs.deepseek.com/guides/json_mode
